import React from 'react';
import ReactDOM from 'react-dom/client';
import ReactRouters from './ReactRouters/ReactRouters';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <div>
    <ReactRouters />
  </div>
);