import React from 'react';
import ReactDOM from 'react-dom/client';
import ReactStates from './ReactStates/ReactStates';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <div>
    <ReactStates />
  </div>
);