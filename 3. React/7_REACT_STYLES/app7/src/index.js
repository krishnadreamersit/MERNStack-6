import React from 'react';
import ReactDOM from 'react-dom/client';
import ReactStyles from './ReactStyles/ReactStyles';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <div>
    <ReactStyles />
  </div>
);