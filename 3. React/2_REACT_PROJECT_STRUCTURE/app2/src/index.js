import React from 'react';
import ReactDOM from 'react-dom/client';
import Components from './HelloWorld/HelloWorld';
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <div>
    <Components/>
  </div>
);