import React from 'react';
import ReactDOM from 'react-dom/client';
import ReactEvent from './ReactEvents/ReactEvents';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <div>
    <ReactEvent />
  </div>
);