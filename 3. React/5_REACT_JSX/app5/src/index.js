import React from 'react';
import ReactDOM from 'react-dom/client';
import ReactJSX from './ReactJSX/ReactJSX';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <div>
    <ReactJSX/>
  </div>
);